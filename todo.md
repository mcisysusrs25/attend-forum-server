// assign batches to the sessoin and test it onnce in dev. 
// remove the  batches from the session. 
// if the session is active and if the session has the batches being used, then admin can not perform crud on that batch. 
// once hte sessoin is acitve, the session is not mutable. // as it sync data to the blockchian. there should not be any way to edit the data. 

// write an api to generate an QR code. 
// generate a digital signature. 
// node mailer to mail the users, once the session is active. 
// URL maker, with digital signatures configured. 
// user opens the URL. it Opens the camera. 
// user scans the QR, access the location info and calls api. if it fits within the radius configured, then it will fetch the student details from the database decrypting the student details using the digital signature. 
// store the data into the blockchain with the public key pair 
// returns the flag, and sabes into the mongodb database. 

// returns the status to the user. 

// hyper ledger sdk setup. 
// smart contract
// deplouyment

// deployment in the VPS or else. 
// need to test and run. 

// flutter app ( ios. andorid ) or provide an api so it can be accessed within the YSU application.